extends ../templateChart
block vars
  - var subheader = "Interaction chart - " + param1 + " vs. " + param2;
  
block script
  script.
    $(function() {
    	var div_id = "#interaction_chart_#{chart_id}";
      $(div_id + " .button").on('click', function() {
        $(div_id).remove();
        pareto_loaded = false;
      });
    });